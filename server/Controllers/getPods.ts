/*

Get a list of pods in the system.$
use kubectl to get pods as a yaml,

All the list items. 
metadata.name



return an array of user deployed pods and the nodes they're in.
[[pod, node]]
[{pod: pod, node: node}, {}]     do this one, it's more descriptive. 

*/